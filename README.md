# Workout Log

筋トレメニューを記録するシンプルな PWA です。

## Features

- 日付ごとのワークアウト記録
- 種目ごとの重量、回数、セット入力
- 過去履歴の一覧表示と詳細確認
- 同じ種目の前回記録表示
- `localStorage` 保存
- PWA 対応

## Local Usage

`index.html` をブラウザで開くと動作します。

## GitHub Pages

静的ファイルだけで構成されているので、そのまま GitHub Pages で公開できます。
